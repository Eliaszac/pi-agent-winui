<UserControl x:Class="PiAgentGui.Controls.FileDiffView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:PiAgentGui.Models.Conversations"
    xmlns:controls="using:PiAgentGui.Controls">
    <Border CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <Grid x:Name="Toolbar" Padding="10,5" Background="{ThemeResource ControlFillColorSecondaryBrush}">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock x:Name="FileTitle" FontSize="11" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="2">
                    <controls:ActionButton Click="OnExpand" Style="{StaticResource ShellIconButtonStyle}" ToolTipService.ToolTip="Open full diff" AutomationProperties.Name="Open full diff"><FontIcon Glyph="&#xE740;" FontSize="12"/></controls:ActionButton>
                    <controls:CopyFeedbackButton Click="OnCopy" Style="{StaticResource ShellIconButtonStyle}" ToolTipService.ToolTip="Copy diff" AutomationProperties.Name="Copy diff"/>
                </StackPanel>
            </Grid>
            <ListView x:Name="Lines" Grid.Row="1" MaxHeight="360" SelectionMode="None" IsItemClickEnabled="False" Padding="0" Background="Transparent"
                ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollMode="Enabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.ItemContainerTransitions><TransitionCollection/></ListView.ItemContainerTransitions>
                <ListView.ItemContainerStyle><Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0"/><Setter Property="MinHeight" Value="20"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListViewItem">
                        <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" HorizontalContentAlignment="Stretch"/>
                    </ControlTemplate></Setter.Value></Setter>
                </Style></ListView.ItemContainerStyle>
                <ListView.ItemTemplate><DataTemplate x:DataType="models:DiffLine">
                    <Grid MinHeight="20">
                        <Border Background="{ThemeResource SystemFillColorSuccessBrush}" Opacity="0.12" Visibility="{x:Bind IsAdded, Converter={StaticResource BooleanToVisibility}}"/>
                        <Border Background="{ThemeResource SystemFillColorCriticalBrush}" Opacity="0.12" Visibility="{x:Bind IsRemoved, Converter={StaticResource BooleanToVisibility}}"/>
                        <Grid Padding="6,1,12,1" Visibility="{x:Bind IsCodeRow, Converter={StaticResource BooleanToVisibility}}">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="44"/><ColumnDefinition Width="44"/><ColumnDefinition Width="22"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{x:Bind OldNumber}" FontFamily="Consolas" FontSize="11" TextAlignment="Right" Margin="0,0,8,0" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Grid.Column="1" Text="{x:Bind NewNumber}" FontFamily="Consolas" FontSize="11" TextAlignment="Right" Margin="0,0,8,0" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Grid.Column="2" Text="{x:Bind Marker}" FontFamily="Consolas" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <controls:DiffCodeText Grid.Column="3" Line="{x:Bind}"/>
                        </Grid>
                        <controls:ActionButton Content="{x:Bind Text}" Tag="{x:Bind}" Click="OnRevealContext" Visibility="{x:Bind CanExpand, Converter={StaticResource BooleanToVisibility}}"
                            HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Padding="10,0" MinHeight="20" Height="20" FontSize="11" BorderThickness="0" Background="Transparent"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}" ToolTipService.ToolTip="Show 15 more unchanged lines"/>
                    </Grid>
                </DataTemplate></ListView.ItemTemplate>
            </ListView>
            <TextBlock x:Name="Notice" Grid.Row="2" Margin="10,6" TextWrapping="Wrap" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Visibility="Collapsed"/>
        </Grid>
    </Border>
</UserControl>
