<UserControl x:Class="PiAgentGui.Views.DockerPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:PiAgentGui.Controls">
    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="Containers" FontWeight="SemiBold" VerticalAlignment="Center"/>
            <controls:ActionButton Grid.Column="1" Command="{Binding RefreshCommand}" ToolTipService.ToolTip="Refresh containers" AutomationProperties.Name="Refresh containers">
                <FontIcon Glyph="&#xE72C;" FontSize="14"/>
            </controls:ActionButton>
            <controls:ActionButton Grid.Column="2" Content="Manage" Click="OnManage"/>
        </Grid>
        <StackPanel Grid.Row="1" Spacing="6">
            <ProgressBar IsIndeterminate="True" Visibility="{Binding Busy, Converter={StaticResource BooleanToVisibility}}"/>
            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" FontSize="12" Foreground="{ThemeResource SystemFillColorCautionBrush}"/>
        </StackPanel>
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="12">
                <ItemsControl ItemsSource="{Binding Sources}">
                    <ItemsControl.ItemTemplate><DataTemplate>
                        <StackPanel Spacing="3" Margin="0,0,0,10">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="12"/>
                            <TextBlock Text="{Binding Status}" TextWrapping="Wrap" IsTextSelectionEnabled="True" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                    </DataTemplate></ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="{Binding EmptyText}" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <ItemsControl ItemsSource="{Binding VisibleContainers}">
                    <ItemsControl.ItemTemplate><DataTemplate>
                        <Border Padding="12" Margin="0,0,0,8" CornerRadius="6" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                            <StackPanel Spacing="6">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding SourceName}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{Binding Image}" FontSize="12" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding Image}"/>
                                <TextBlock Text="{Binding Status}" FontSize="12" TextWrapping="Wrap"/>
                                <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,4,0,0">
                                    <controls:ActionButton Content="Start" Command="{Binding StartCommand}" IsEnabled="{Binding CanStart}"/>
                                    <controls:ActionButton Content="Stop" Command="{Binding StopCommand}" IsEnabled="{Binding CanStop}"/>
                                    <ProgressRing Width="18" Height="18" IsActive="{Binding Busy}" Visibility="{Binding Busy, Converter={StaticResource BooleanToVisibility}}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate></ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
